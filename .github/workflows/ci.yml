name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Arduino CLI
      uses: arduino/setup-arduino-cli@v2
      
    - name: Install platform
      run: |
        arduino-cli core update-index --additional-urls https://arduino.esp8266.com/stable/package_esp8266com_index.json,https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
        arduino-cli core install esp8266:esp8266 --additional-urls https://arduino.esp8266.com/stable/package_esp8266com_index.json
        arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
        
    - name: Install libraries
      run: |
        arduino-cli lib install "TinyGPSPlus"
        arduino-cli lib install "MPU6050"
    
    - name: Lint code
      run: |
        find src -name "*.ino" -o -name "*.h" -o -name "*.cpp" | xargs -I {} echo "Checking {}"
        # Basic syntax checking
        arduino-cli compile --fqbn esp8266:esp8266:nodemcuv2 src/esp8266-nodes/transmitter/
        arduino-cli compile --fqbn esp8266:esp8266:nodemcuv2 src/esp8266-nodes/receiver/
        arduino-cli compile --fqbn esp32:esp32:devkitv1 src/esp32-main-controller/

  test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Run unit tests
      run: |
        python -m pytest tests/unit_tests/ -v --cov=src
    
    - name: Run integration tests
      run: |
        python -m pytest tests/integration_tests/ -v

  build:
    runs-on: ubuntu-latest
    needs: [lint, test]
    strategy:
      matrix:
        board: 
          - esp8266:esp8266:nodemcuv2
          - esp32:esp32:devkitv1
        sketch:
          - src/esp8266-nodes/transmitter/
          - src/esp8266-nodes/receiver/
          - src/esp32-main-controller/
        exclude:
          - board: esp32:esp32:devkitv1
            sketch: src/esp8266-nodes/transmitter/
          - board: esp32:esp32:devkitv1
            sketch: src/esp8266-nodes/receiver/
          - board: esp8266:esp8266:nodemcuv2
            sketch: src/esp32-main-controller/
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Arduino CLI
      uses: arduino/setup-arduino-cli@v2
      
    - name: Install platform
      run: |
        arduino-cli core update-index --additional-urls https://arduino.esp8266.com/stable/package_esp8266com_index.json,https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
        arduino-cli core install ${{ matrix.board }} --additional-urls https://arduino.esp8266.com/stable/package_esp8266com_index.json,https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
        
    - name: Install libraries
      run: |
        arduino-cli lib install "TinyGPSPlus"
        arduino-cli lib install "MPU6050"
    
    - name: Compile sketch
      run: |
        arduino-cli compile --fqbn ${{ matrix.board }} --output-dir ${{ matrix.sketch }}build ${{ matrix.sketch }}
    
    - name: Set safe board name
      run: |
        BOARD_SAFE="${{ matrix.board }}"
        echo "BOARD_SAFE=${BOARD_SAFE//:/_}" >> $GITHUB_ENV

    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        name: firmware-${{ env.BOARD_SAFE }}-${{ github.sha }}
        path: |
          ${{ matrix.sketch }}build/**/*.bin
          ${{ matrix.sketch }}build/**/*.hex

  documentation:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install documentation tools
      run: |
        pip install mkdocs mkdocs-material
        sudo apt-get install doxygen
    
    - name: Generate documentation
      run: |
        # Generate API docs from source code
        doxygen docs/Doxyfile
        # Build documentation site
        mkdocs build
    
    - name: Deploy to GitHub Pages
      if: github.ref == 'refs/heads/main'
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./site

  security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Run security scan
      run: |
        # Check for hardcoded secrets
        find src -name "*.ino" -o -name "*.h" | xargs grep -l "password\|secret\|key" || true
        
        # Check for vulnerable dependencies
        pip install safety
        safety check --json || true

  release:
    runs-on: ubuntu-latest
    needs: [build, test, documentation]
    if: github.ref == 'refs/heads/main' && startsWith(github.ref, 'refs/tags/')
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Download artifacts
      uses: actions/download-artifact@v3
    
    - name: Create Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: DrishtiGuide v${{ github.ref_name }}
        draft: false
        prerelease: false
    
    - name: Upload Release Assets
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./firmware-esp8266_esp8266_nodemcuv2-${{ github.sha }}/transmitter.ino.bin
        asset_name: transmitter_firmware.bin
        asset_content_type: application/octet-stream